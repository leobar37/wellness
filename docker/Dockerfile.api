FROM node:16-alpine AS dependeces

WORKDIR /build

COPY package.json ./

COPY yarn.lock ./

RUN yarn install

COPY . .

RUN yarn nx build api --configuration=production

ENV PORT=3500

ENV NODE_ENV=production

EXPOSE ${PORT}

CMD ["yarn", "nx" ,"serve" , ""]

